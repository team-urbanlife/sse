spring:
  config:
    activate:
      on-profile: local

  application:
    name: wegotoo_sse

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    redis:
      host: localhost
      port: 6379